# Override system nim.cfg's "-Wl,-Bstatic -lpthread" for this daemon binary.
# asyncdispatch on Windows uses IOCP (not pthreads), so dynamic winpthread is fine.
@if gcc:
  gcc.options.linker = "-lwinpthread"
@end

path = "../src"
# Release mode so test compilation shares the nimcache with the daemon
# pre-build step in CI.  All tests use check() from unittest (not assert),
# so disabling Nim's system assertions has no effect on correctness.
-d:release
@if gcc and windows:
  gcc.options.linker = "-lwinpthread"
@end
